{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "updateAWSADFS",
            "type": "shell",
            "command": "aws-adfs login --adfs-host=adfs --no-ssl-verification --region us-east-1",
            "problemMatcher": []
        }
    ]
}